Run the verification gate:

```bash
cd $PROJECT_ROOT
make verify
```

If it fails, read the error output carefully, fix the root cause, then run again.
If it fails twice on the same error, stop and explain why — do not loop indefinitely.
